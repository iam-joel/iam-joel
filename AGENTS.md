# iam-joel

GitHub profile README repo: `README.md` is the only content (bio, badges, typing SVG, skill icons, stats widgets), rendered on https://github.com/iam-joel.

- Content-only, ever: no `package.json`, lockfiles, `src/`, CI workflows, or build tooling. Nothing to install, build, lint, or test.
- Edit `README.md` in place; keep its HTML+Markdown mix, no rewrite into a plain docs layout.
- The top badge reads "README Refreshed <Mon YYYY>". Whenever the README changes, set it to the current month (badge URL and `alt` text).
- `.github/CODEOWNERS` stays as-is unless asked.
