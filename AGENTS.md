# Agent notes — iam-joel

This repo is **only** the GitHub profile README for [`iam-joel`](https://github.com/iam-joel).

## Scope

- Edit `README.md` for profile presentation (badges, HTML blocks, typing SVG, skill icons).
- Do **not** turn this into an app, site, package, or installable project.
- No `package.json`, installs, CI, or build tooling.

## Style

- Preserve the existing badge / centered HTML / Markdown mix in `README.md`.
- Prefer small, visual-friendly edits over rewriting the whole profile.
- Do not gut or replace the profile README with a plain docs layout.
